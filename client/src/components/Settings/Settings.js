import React from 'react';
import './Settings.css';

const Settings = () => (
    <div className="Settings">
        Settings Component
    </div>
);

Settings.propTypes = {};

Settings.defaultProps = {};

export default Settings;
